name: Validate rules and templates

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: python -m pip install --upgrade pip pyyaml
      - name: Check generated crypto/Web3 supplement
        run: python scripts/build_crypto_custom.py --check
      - name: Validate rules and V3 template
        run: python scripts/validate.py
